# Backend (Express API)
PORT=5176
JWT_SECRET=change-me-in-production

# Optional: seed an initial admin account on server start
# If not provided, no admin user is auto-created.
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Vite dev origin (used by backend for Stripe redirects if Origin header is missing)
CORS_ORIGIN=http://localhost:5173

# Stripe (Test mode)
# Get from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...

# Optional (only needed if you test webhooks locally)
# Get from: stripe listen --forward-to localhost:5176/api/payments/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_...
